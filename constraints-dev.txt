# Optional version pins for a more reproducible dev/CI environment.
#
# Used via: pip install -c constraints-dev.txt -e '.[dev]'

numpy==2.2.6
pytest==9.0.2
ruff==0.15.1
matplotlib==3.10.8
jupyter==1.1.1
pyyaml==6.0.3

# Optional extras (not installed in the default dev set, but pinned here so CI is reproducible
# when we test optional JAX paths).

jax==0.6.2
jaxlib==0.6.2
blackjax==1.2.5

# "modern-sbi" stack + key transitive deps (so `pip -c constraints-dev.txt` is stable).

equinox==0.13.4
optax==0.2.7
distrax==0.1.5
chex==0.1.90
tensorflow-probability==0.25.0
dm-tree==0.1.9
jaxtyping==0.3.7
wadler-lindig==0.1.7
absl-py==2.4.0
cloudpickle==3.1.2
decorator==5.2.1
gast==0.7.0
six==1.17.0
toolz==1.1.0
typing-extensions==4.15.0
wrapt==2.1.1
attrs==25.4.0
